<% tasks.each_with_index do |task, index| %>
    <%= render 'tasks/task', task: task, task_index: index %>
    <hr>
<% end %>
<%= link_to 'Add task', new_task_path(project_id: project_id), class: 'has-text-success', remote: true unless project_id.nil? %>